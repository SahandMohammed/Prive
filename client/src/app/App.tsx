// Placeholder — replace with your first real page once feature pages exist.
// The router in router.tsx maps '/' here.
export default function App() {
  return (
    <div className="flex min-h-screen items-center justify-center bg-gray-50">
      <p className="text-gray-500 text-sm">Prive — app shell ready</p>
    </div>
  )
}
